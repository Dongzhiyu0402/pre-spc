[project]
name = "pre-spc-backend"
version = "0.1.0"
description = "预查重项目 FastAPI 后端"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]

[tool.ruff]
line-length = 120
target-version = "py310"
