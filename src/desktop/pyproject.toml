[project]
name = "pre-spc-desktop"
version = "0.1.0"
description = "预查重项目 PyQt6 桌面端（离线引擎 + 在线 API 客户端）"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.6",
    "httpx>=0.27",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
